<UserControl x:Class="Desktop.Wpf.Features.Shell.LoadingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="{StaticResource SurfaceBrush}">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="260" TextBlock.TextAlignment="Center">
      <TextBlock Margin="0,0,0,8" Foreground="{StaticResource OnSurfaceVariantBrush}" Text="Loading..."/>
      <ProgressBar IsIndeterminate="True" Height="4"/>
    </StackPanel>
  </Grid>
</UserControl>
