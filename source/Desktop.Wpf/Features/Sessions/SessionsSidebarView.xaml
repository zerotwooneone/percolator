<UserControl x:Class="Desktop.Wpf.Features.Sessions.SessionsSidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:att="clr-namespace:Desktop.Wpf.Shared.Attached">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Margin="16,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <!-- Leading search icon -->
      <TextBlock Grid.Column="0" Style="{StaticResource IconText}" Text="{StaticResource IconSearch}" Margin="8,0,8,0" Opacity="0.8"/>
      <!-- Search box fills -->
      <TextBox Grid.Column="1" Style="{StaticResource WatermarkTextBox}"
               att:Placeholder.Text="Search encrypted logs..."
               Padding="28,6,8,6"
               Text="{Binding SearchText.Value, UpdateSourceTrigger=PropertyChanged}"/>
      <!-- Trailing pencil action -->
      <Button Grid.Column="2" Style="{StaticResource BaseButton}" Width="32" Height="32" Margin="8,0,0,0" Padding="0">
        <TextBlock Style="{StaticResource IconText}" Text="{StaticResource IconPencil}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Button>
    </Grid>

    <ListBox Grid.Row="1" ItemsSource="{Binding Items}" SelectedValuePath="Id" SelectedValue="{Binding SelectedSessionId.Value, Mode=TwoWay}">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <Border Style="{StaticResource BaseBorder}" Margin="12,8,12,0">
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
              </Grid.ColumnDefinitions>
              <Border Width="36" Height="36" CornerRadius="18" Background="#2EC7FF33">
                <TextBlock Text="{Binding Initials}" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
              </Border>
              <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="{Binding DisplayName}"/>
                <TextBlock Text="{Binding LastMessagePreview}" Foreground="{StaticResource MutedBrush}" FontSize="12"/>
              </StackPanel>
              <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Right">
                <TextBlock Text="{Binding TimestampText}" Foreground="{StaticResource MutedBrush}" FontSize="12" HorizontalAlignment="Right"/>
                <Border x:Name="UnreadBadge" Background="{StaticResource AccentBrush}" CornerRadius="10" Padding="6,2" Margin="0,6,0,0" Visibility="Collapsed">
                  <Border.Style>
                    <Style TargetType="Border">
                      <Setter Property="Visibility" Value="Collapsed"/>
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding HasUnread}" Value="True">
                          <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </Border.Style>
                  <TextBlock Text="{Binding UnreadCount}" Foreground="#001018" FontWeight="SemiBold" FontSize="12"/>
                </Border>
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </Grid>
</UserControl>
