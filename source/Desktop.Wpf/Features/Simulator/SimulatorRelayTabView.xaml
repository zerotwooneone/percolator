<UserControl x:Class="Desktop.Wpf.Features.Simulator.SimulatorRelayTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <UserControl.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
  </UserControl.Resources>

  <Grid Margin="16" RowDefinitions="Auto,*">
    <Grid Grid.Row="0" ColumnDefinitions="*,Auto" Margin="0,0,0,12">
      <TextBlock Grid.Column="0" FontSize="18" FontWeight="SemiBold" Text="Relay Queues" VerticalAlignment="Center" />
      <CheckBox Grid.Column="1" Content="Global Auto-Relay All" IsChecked="{Binding GlobalAutoRelayAll.Value}" VerticalAlignment="Center" />
    </Grid>

    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding RelayPanels}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Margin="0,0,0,12" Padding="16" BorderThickness="1" BorderBrush="#333" CornerRadius="8" HorizontalAlignment="Stretch">
              <StackPanel>
                <Grid ColumnDefinitions="Auto,Auto,*,Auto,Auto,Auto" VerticalAlignment="Center">
                  <TextBlock Grid.Column="0" FontSize="15" FontWeight="SemiBold" Text="{Binding RelayHostName}" VerticalAlignment="Center" />
                  <Border Grid.Column="1" Margin="10,0,0,0" Padding="8,2" CornerRadius="10" Background="#222" VerticalAlignment="Center">
                    <TextBlock FontSize="11" Foreground="#AAA" Text="{Binding QueueCount.Value, StringFormat={}{0} in queue}" />
                  </Border>

                  <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <CheckBox Content="Auto-Deliver" IsChecked="{Binding AutoDeliver.Value}" />
                  </StackPanel>
                  <Button Grid.Column="4" Margin="10,0,0,0" Content="Next" Command="{Binding NextCommand}" />
                  <Button Grid.Column="5" Margin="8,0,0,0" Content="All" Command="{Binding AllCommand}" />
                </Grid>

                <ItemsControl Margin="0,12,0,0" ItemsSource="{Binding Items}">
                  <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                      <StackPanel />
                    </ItemsPanelTemplate>
                  </ItemsControl.ItemsPanel>
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Border Margin="0,0,0,10" Padding="12" BorderThickness="1" BorderBrush="#2A2A2A" CornerRadius="6">
                        <Grid ColumnDefinitions="*,Auto">
                          <StackPanel Grid.Column="0">
                            <TextBlock FontSize="11" Foreground="#888">
                              <Run Text="{Binding TimestampDisplay}" />
                              <Run Text="  |  " />
                              <Run Text="{Binding TypeLabel}" />
                            </TextBlock>
                            <TextBlock Margin="0,4,0,0" FontSize="13" Foreground="#67D7FF" Text="{Binding FromToDisplay}" />
                          </StackPanel>

                          <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Deliver"
                                    Command="{Binding DataContext.DeliverItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Margin="8,0,0,0" Content="Up"
                                    Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Margin="8,0,0,0" Content="Down"
                                    Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Margin="8,0,0,0" Content="Corrupt"
                                    Command="{Binding DataContext.CorruptCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                            <Button Margin="8,0,0,0" Content="Drop"
                                    Command="{Binding DataContext.DropItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}" />
                          </StackPanel>
                        </Grid>
                      </Border>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
  </Grid>
</UserControl>
