<UserControl x:Class="Desktop.Wpf.Features.Chat.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Messages -->
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
      <ItemsControl ItemsSource="{Binding Messages}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <DockPanel Margin="12,6">
              <Border DockPanel.Dock="Right" Background="#203047" Padding="10" CornerRadius="8">
                <TextBlock Text="{Binding Text}"/>
              </Border>
            </DockPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>

    <!-- Composer -->
    <DockPanel Grid.Row="1" Margin="12" LastChildFill="False">
      <TextBox Style="{StaticResource BaseTextBox}" Width="Auto" MinHeight="36" Margin="0,0,8,0"
               Text="{Binding MessageInput.Value, UpdateSourceTrigger=PropertyChanged}"/>
      <Button Content="Send" IsEnabled="{Binding CanSend.Value}" Command="{Binding SendCommand}"/>
    </DockPanel>
  </Grid>
</UserControl>
